# P0 — 启动层（阻塞所有后续工作）

> 完成每个任务后，**同时更新 [INDEX.md](INDEX.md) 对应行的 `[ ]` → `[x]`**，并更新总进度计数。

---

## P0-01 更新 composer.json

**目标文件**：`composer.json`

**改动清单**：

1. `require` 中添加（删除旧的 karsonzhang，升级 think-* 到 3.x）：
```json
"topthink/framework": "^8.1",
"topthink/think-multi-app": "^1.0",
"topthink/think-captcha": "^3.0",
"topthink/think-helper": "^3.0",
"topthink/think-image": "^2.0"
```
2. `require` 中删除：`"karsonzhang/fastadmin-addons": "1.1.11"`
3. `autoload.psr-4` 中删除：`"think\\": "thinkphp/library/think/"` 这一条（TP8 后 thinkphp/ 目录会删除）
4. `autoload.psr-4` 保留：`"app\\": "application/"` / `"Qiniu\\"` / `"Upyun\\"` / `"PHPMailer\\PHPMailer\\"`
5. `autoload.psr-4` 新增：`"addons\\": "addons/"` （addon 目录独立命名空间）

**验证**：`php composer.phar validate` 无错误

**完成后**：更新 INDEX.md P0-01 → [x]

---

## P0-02 运行 composer update

**命令**：
```bash
COMPOSER_ALLOW_SUPERUSER=1 php composer.phar update \
  --ignore-platform-req=ext-gd \
  --ignore-platform-req=ext-mbstring \
  --no-dev
```

**验证**：
```bash
php -r "require 'vendor/autoload.php'; echo \think\App::VERSION . PHP_EOL;"
# 输出 8.x.x
```

**完成后**：更新 INDEX.md P0-02 → [x]

---

## P0-03 创建 config/app.php

**目标文件**：`config/app.php`（新建）

**内容要点**：
- `'app_namespace' => 'app'`
- `'app_debug' => false`（从 application/config.php 的 `app_debug` 读取初始值）
- `'default_timezone' => 'Asia/Shanghai'`
- 多应用支持：TP8 多应用由 `topthink/think-multi-app` 包处理，此处只需正确的 `app_path`
- `'app_path'` 不在 config/app.php 里写（在入口文件 `new App(APP_PATH)` 传入），此处留空

完整示例：
```php
<?php
return [
    'app_namespace'      => 'app',
    'app_debug'          => false,
    'app_trace'          => false,
    'default_timezone'   => 'Asia/Shanghai',
    'default_lang'       => 'zh-cn',
    'exception_handle'   => '',
    'http_exception_template' => [],
];
```

**完成后**：更新 INDEX.md P0-03 → [x]

---

## P0-04 创建 config/database.php

**目标文件**：`config/database.php`（新建）

**来源**：`application/database.php`

**必须修正的键名差异（TP5 → TP8）**：

| TP5 键名 | TP8 键名 | 说明 |
|----------|----------|------|
| `resultset_type` | `result_type` | 0=数组, 1=对象 |
| `fields_strict` | 删除 | TP8 无此配置 |
| `builder` | 删除 | TP8 无此配置 |
| `query` | 删除 | TP8 无此配置 |
| `auto_timestamp` | 保留，但改为 `false` 或 `'datetime'` | TP8 布尔或字符串 |

TP8 database.php 基础结构：
```php
<?php
return [
    'default'     => 'mysql',
    'connections' => [
        'mysql' => [
            'type'            => 'mysql',
            'hostname'        => env('DB_HOST', '127.0.0.1'),
            'database'        => env('DB_NAME', ''),
            'username'        => env('DB_USER', ''),
            'password'        => env('DB_PASS', ''),
            'hostport'        => '3306',
            'charset'         => 'utf8mb4',
            'prefix'          => 'mac_',
            'result_type'     => 0,    // 0=数组
            'auto_timestamp'  => false,
            'datetime_format' => 'Y-m-d H:i:s',
            'fields_cache'    => false,
        ],
    ],
];
```

**注意**：`application/database.php` 中的实际凭证需要原样复制。

**完成后**：更新 INDEX.md P0-04 → [x]

---

## P0-05 创建 config/cache.php

**目标文件**：`config/cache.php`（新建）

**注意**：TP8 的 cache 配置支持多 store。运行时动态切换（Redis/File）由 Init Middleware（P0-15）通过 `Config::set` 写入，此处只写默认值：

```php
<?php
return [
    'default' => 'file',
    'stores'  => [
        'file' => [
            'type'   => 'file',
            'path'   => '',
            'prefix' => '',
            'expire' => 0,
        ],
        'redis' => [
            'type'     => 'redis',
            'host'     => '127.0.0.1',
            'port'     => 6379,
            'password' => '',
            'select'   => 0,
            'timeout'  => 0,
            'expire'   => 0,
            'prefix'   => '',
        ],
    ],
];
```

**完成后**：更新 INDEX.md P0-05 → [x]

---

## P0-06 创建 config/session.php

**目标文件**：`config/session.php`（新建）

从 `application/config.php` 的 `session` 段提取，TP8 键名基本不变：

```php
<?php
return [
    'type'     => 'file',
    'expire'   => 1440,
    'prefix'   => 'mac_',
    'httponly' => true,
    'secure'   => false,
    'samesite' => 'Lax',
    'domain'   => '',
    'path'     => '/',
];
```

运行时 Redis session 切换在 Init Middleware 中用 `Config::set` 完成。

**完成后**：更新 INDEX.md P0-06 → [x]

---

## P0-07 创建 config/view.php

**目标文件**：`config/view.php`（新建）

关键：保留 `taglib_pre_load`，确保自定义 TagLib 加载。

```php
<?php
return [
    'type'            => 'Think',
    'view_path'       => '',          // 由 Init Middleware 运行时写入
    'view_suffix'     => 'html',
    'view_depr'       => DIRECTORY_SEPARATOR,
    'tpl_begin'       => '{',
    'tpl_end'         => '}',
    'taglib_begin'    => '{',
    'taglib_end'      => '}',
    'taglib_pre_load' => 'app\\common\\taglib\\Maccms',
    'tpl_cache'       => true,
    'cache_path'      => '',
    'cache_suffix'    => 'php',
    'cache_prefix'    => '',
    'display_cache'   => false,
    'cache_id'        => '',
    'tpl_deny_func_list' => 'echo,exit,die',
    'tpl_deny_php'    => false,
];
```

**完成后**：更新 INDEX.md P0-07 → [x]

---

## P0-08 创建 config/log.php

**目标文件**：`config/log.php`（新建）

```php
<?php
return [
    'default'      => 'file',
    'channels' => [
        'file' => [
            'type'         => 'file',
            'path'         => '',
            'single'       => false,
            'apart_level'  => ['error', 'sql'],
            'max_files'    => 0,
            'file_size'    => 2097152,
            'json'         => false,
            'format'       => '[%s][%s] %s',
            'realtime_write' => false,
        ],
    ],
];
```

**完成后**：更新 INDEX.md P0-08 → [x]

---

## P0-09 改造 index.php

**目标文件**：`index.php`

用以下内容替换 `require __DIR__ . '/thinkphp/start.php';` 及其周边的 TP5 启动逻辑（保留常量定义）：

```php
// 加载框架引导文件（TP8）
require __DIR__ . '/vendor/autoload.php';
$app = new \think\App(APP_PATH);
$http = $app->http;
$response = $http->name('index')->run();
$response->send();
$http->end($response);
```

删除旧的 PHP 版本检测（5.5 检测无意义，现在要求 PHP 8.2+），改为：
```php
if (version_compare(PHP_VERSION, '8.0.0', '<')) {
    die('PHP >= 8.0 required');
}
```

**完成后**：更新 INDEX.md P0-09 → [x]

---

## P0-10 改造 admin.php

**目标文件**：`admin.php`

同 P0-09，替换启动段：
```php
require __DIR__ . '/vendor/autoload.php';
$app = new \think\App(APP_PATH);
$http = $app->http;
$response = $http->name('admin')->run();
$response->send();
$http->end($response);
```

**注意**：`define('ENTRANCE', 'admin')` 保留，Begin Middleware 中仍依赖此常量。

**完成后**：更新 INDEX.md P0-10 → [x]

---

## P0-11 改造 api.php

**目标文件**：`api.php`

```php
require __DIR__ . '/vendor/autoload.php';
$app = new \think\App(APP_PATH);
$http = $app->http;
$response = $http->name('api')->run();
$response->send();
$http->end($response);
```

**完成后**：更新 INDEX.md P0-11 → [x]

---

## P0-12 Shim 函数写入 application/common.php

**目标文件**：`application/common.php`（追加到文件末尾，`<?php` 已存在）

追加以下代码块（用 `function_exists` 守护，保证幂等）：

```php
// ========= TP8 过渡 Shim — 待各模块改写完成后逐步移除 =========
if (!function_exists('model')) {
    /**
     * @deprecated 迁移期 shim，用 new \app\common\model\Xxx() 替代
     */
    function model(string $name, string $layer = 'model'): object {
        $class = '\\app\\common\\' . $layer . '\\' . ucfirst($name);
        return new $class();
    }
}
if (!function_exists('input')) {
    /**
     * @deprecated 迁移期 shim，用 request()->param/post/get() 替代
     */
    function input(string $key = '', $default = null, string $filter = '') {
        return \think\facade\Request::input($key, $default, $filter);
    }
}
// url() 全局函数：委托 mac_url()，让 206 个模板的 {:url(} 零改动
if (!function_exists('url')) {
    function url(string $url = '', array $vars = [], bool $suffix = true, bool $domain = false): string {
        return mac_url($url, $vars);
    }
}
// ========= /TP8 过渡 Shim =========
```

**验证**：`php -r "require 'vendor/autoload.php'; echo function_exists('model') ? 'ok' : 'fail';"` 输出 ok

**完成后**：更新 INDEX.md P0-12 → [x]

---

## P0-13 改造 All.php Controller Shim

**目标文件**：`application/common/controller/All.php`

**改动 1**：顶部 use 声明改为 TP8 facade：
```php
// 删除
use think\Controller;
use think\Cache;
use think\Request;

// 改为
use think\facade\Cache;
use think\facade\Request;
// think\Controller 在 TP8 中仍存在，暂时保留 extends Controller
```

**改动 2**：追加 TP5 兼容方法（加在类的末尾，`__construct` 不动）：
```php
    // ===== TP8 Controller Shim（P5 阶段移除）=====
    protected function success($msg = '', $url = null, $data = '', $wait = 3) {
        if (\think\facade\Request::isAjax()) {
            return json(['code' => 1, 'msg' => $msg, 'data' => $data]);
        }
        $this->assign(['msg' => $msg, 'url' => $url ?? 'javascript:history.back();', 'wait' => $wait, 'type' => 'success']);
        throw new \think\exception\HttpResponseException(\think\facade\View::fetch('public/jump'));
    }

    protected function error($msg = '', $url = null, $data = '', $wait = 3) {
        if (\think\facade\Request::isAjax()) {
            return json(['code' => 0, 'msg' => $msg, 'data' => $data]);
        }
        $this->assign(['msg' => $msg, 'url' => $url ?? 'javascript:history.back();', 'wait' => $wait, 'type' => 'error']);
        throw new \think\exception\HttpResponseException(\think\facade\View::fetch('public/jump'));
    }

    protected function assign($name, $value = ''): void {
        \think\facade\View::assign(is_array($name) ? $name : [$name => $value]);
    }

    protected function fetch(string $template = '', array $vars = []): string {
        if ($vars) {
            \think\facade\View::assign($vars);
        }
        return \think\facade\View::fetch($template);
    }
    // ===== /TP8 Controller Shim =====
```

**注意**：TP8 的 `think\Controller` 基类中已不存在 `$this->view` 属性，上面的 `assign/fetch` 直接调用 `View` facade 绕过这个问题。

**完成后**：更新 INDEX.md P0-13 → [x]

---

## P0-14 SessionSameSite → Middleware

**来源**：`application/common/behavior/SessionSameSite.php`
**目标**：`application/middleware/SessionSameSite.php`（新建目录 application/middleware/）

**改动**：将 `run(&$params)` 方法体包装为 TP8 Middleware 格式：

```php
<?php
namespace app\middleware;

class SessionSameSite
{
    public function handle($request, \Closure $next)
    {
        // （原 SessionSameSite::run() 的方法体，原样搬入）
        if (PHP_SAPI !== 'cli' && PHP_VERSION_ID >= 70300 && !headers_sent() && session_status() !== PHP_SESSION_ACTIVE) {
            // ... 原代码 ...
        }
        return $next($request);
    }
}
```

**验证点**：原代码中用 `config('session')` 读取配置，TP8 中 `config('session')` 读函数仍可用（TP8 保留了 `config()` 只读版本），无需修改。

**完成后**：更新 INDEX.md P0-14 → [x]

---

## P0-15 Init → Middleware

**来源**：`application/common/behavior/Init.php`（157 行）
**目标**：`application/middleware/AppInit.php`

**主要改动**：

1. 方法签名：`run(&$params)` → `handle($request, \Closure $next)`，末尾加 `return $next($request);`

2. `use think\Cache` → `use think\facade\Cache`

3. **27 处 `config()` 写模式全部改为 `\think\facade\Config::set()`**：

| 原写法 | 改为 |
|--------|------|
| `config('dispatch_success_tmpl', 'public/jump')` | 删除（TP8 无此配置，success/error 由 Controller Shim 处理） |
| `config('dispatch_error_tmpl', 'public/jump')` | 删除 |
| `config('template.view_path', $path)` | `\think\facade\Config::set('view.view_path', $path)` |
| `config('url_route_on', $val)` | 删除（TP8 路由默认开启） |
| `config('pathinfo_depr', $val)` | `\think\facade\Config::set('route.pathinfo_depr', $val)` |
| `config('cache.expire', $val)` | `\think\facade\Config::set('cache.stores.file.expire', $val)` |
| `config('cache.type', $val)` | `\think\facade\Config::set('cache.default', $val)` |
| `config('cache.host', $val)` | `\think\facade\Config::set('cache.stores.redis.host', $val)` |
| `config('cache.port', $val)` | `\think\facade\Config::set('cache.stores.redis.port', $val)` |
| `config('cache.username', $val)` | `\think\facade\Config::set('cache.stores.redis.username', $val)` |
| `config('cache.password', $val)` | `\think\facade\Config::set('cache.stores.redis.password', $val)` |
| `config('cache.select', $val)` | `\think\facade\Config::set('cache.stores.redis.select', $val)` |
| `config('cache.timeout', $val)` | `\think\facade\Config::set('cache.stores.redis.timeout', $val)` |
| `config('session.type', 'redis')` | `\think\facade\Config::set('session.type', 'redis')` |
| `config('session.host', $val)` | `\think\facade\Config::set('session.host', $val)` |
| `config('session.port', $val)` | `\think\facade\Config::set('session.port', $val)` |
| `config('session.password', $val)` | `\think\facade\Config::set('session.password', $val)` |
| `config('session.select', $val)` | `\think\facade\Config::set('session.select', $val)` |
| `config('session.timeout', $val)` | `\think\facade\Config::set('session.timeout', $val)` |
| `config('session.persistent', $val)` | `\think\facade\Config::set('session.persistent', $val)` |
| `config('default_lang', $val)` | `\think\facade\Config::set('app.default_lang', $val)` |

4. `config()` 只读调用（如 `config('maccms')`）**不需要改**，TP8 的 `config()` 只读版本仍可用。

**完成后**：更新 INDEX.md P0-15 → [x]

---

## P0-16 RequestSecurity → Middleware

**来源**：`application/common/behavior/RequestSecurity.php`（44 行）
**目标**：`application/middleware/RequestSecurity.php`

`run(&$params)` 方法体原样搬入，无 `use think\*` 依赖，**几乎零改动**。

```php
<?php
namespace app\middleware;

use app\common\util\RequestXssSanitizer;

class RequestSecurity
{
    public function handle($request, \Closure $next)
    {
        // 原 run() 方法体
        $app = isset($GLOBALS['config']['app']) ? ... // 原代码
        // ...
        return $next($request);
    }
}
```

**完成后**：更新 INDEX.md P0-16 → [x]

---

## P0-17 Begin → Middleware

**来源**：`application/common/behavior/Begin.php`（75 行）
**目标**：`application/middleware/Begin.php`

**关键改动**：
```php
// TP5
$dispatch = request()->dispatch();
if (isset($dispatch['module'])) { $module = $dispatch['module'][0]; }

// TP8 — dispatch() 不再返回数组，改为：
$module = $request->module() ?? '';
// 或者直接读当前模块：
$module = app('http')->getName() ?: '';
```

`url('admin/index/index')` 函数调用：TP8 中 `url()` 的参数格式有变化，改为：
```php
\think\facade\Route::buildUrl('admin/index/index')->build()
// 或使用已注册的 url() shim，直接调用 url('admin/index/index') 应仍可用
```

**完成后**：更新 INDEX.md P0-17 → [x]

---

## P0-18 CsrfGuard → Middleware

**来源**：`application/common/behavior/CsrfGuard.php`（202 行）
**目标**：`application/middleware/CsrfGuard.php`

**use 声明改动**：
```php
// 删除
use think\Session;
use think\Request;
use think\Response;
// 改为
use think\facade\Session;
use think\facade\Request;
```

`$response = Response::create(...)` → `$response = response(...)` 或直接返回 `json(['code'=>0,'msg'=>'...'])`。

方法签名：
```php
public function handle($request, \Closure $next) {
    // 原 CsrfGuard run() 逻辑
    // 把原来 "return false" / "throw HttpResponseException" 的拦截路径保留
    return $next($request);
}
```

**完成后**：更新 INDEX.md P0-18 → [x]

---

## P0-19 AntiScrape → Middleware

**来源**：`application/common/behavior/AntiScrape.php`（169 行）
**目标**：`application/middleware/AntiScrape.php`

**use 声明改动**：
```php
use think\exception\HttpResponseException; // 保留（TP8 仍有）
use think\Request; → use think\facade\Request;
```

方法签名同上，末尾 `return $next($request);`。

**完成后**：更新 INDEX.md P0-19 → [x]

---

## P0-20 SecurityHeaders → Middleware

**来源**：`application/common/behavior/SecurityHeaders.php`（84 行）
**目标**：`application/middleware/SecurityHeaders.php`

**特殊处理**：此 Behavior 在 `app_end` 钩子执行，处理的是 **Response 对象**，不是 Request。在 TP8 中用 middleware 处理 Response 的方式：

```php
public function handle($request, \Closure $next)
{
    $response = $next($request);  // 先执行后续中间件
    // 对 $response 追加 header
    // 原 SecurityHeaders::run($response) 的逻辑，操作对象从 $params 改为 $response
    $response->header([
        'X-Content-Type-Options' => 'nosniff',
        // ... 原来的 header 设置
    ]);
    return $response;
}
```

**use 声明**：`use think\Response` → `use think\facade\Response`（如有）

**完成后**：更新 INDEX.md P0-20 → [x]

---

## P0-21 AdminAudit → Middleware

**来源**：`application/common/behavior/AdminAudit.php`（159 行）
**目标**：`application/middleware/AdminAudit.php`

同 SecurityHeaders，在 `$next($request)` 之后处理 Response：

```php
public function handle($request, \Closure $next)
{
    $response = $next($request);
    // 原审计日志逻辑（记录管理员操作）
    // use think\Request → use think\facade\Request
    // use think\Response → 不需要，直接操作 $response
    return $response;
}
```

**完成后**：更新 INDEX.md P0-21 → [x]

---

## P0-22 创建 application/middleware.php

**目标文件**：`application/middleware.php`（新建）

TP8 全局 middleware 配置，按执行顺序注册：

```php
<?php
return [
    \app\middleware\SessionSameSite::class,
    \app\middleware\AppInit::class,
    \app\middleware\RequestSecurity::class,
    \app\middleware\Begin::class,
    \app\middleware\CsrfGuard::class,
    \app\middleware\AntiScrape::class,
    \app\middleware\SecurityHeaders::class,
    \app\middleware\AdminAudit::class,
];
```

**完成后**：更新 INDEX.md P0-22 → [x]

---

## P0-23 内化 fastadmin-addons

**来源**：`vendor/karsonzhang/fastadmin-addons/src/`（4 个文件）
**目标**：`application/common/addons/`（新建目录）

### 23a：复制并改造 Addons.php（基类）

来源：`vendor/karsonzhang/fastadmin-addons/src/Addons.php`
目标：`application/common/addons/Addons.php`

**改动**：
```php
// 保持命名空间为 think（让现有 addon 的 use think\Addons 无需修改）
namespace think;

use think\facade\Config;  // 原 use think\Config
use think\facade\View;    // 原 use think\View
```

构造函数中：
```php
// 原
$config = array_merge(Config::get('template'), $config);
$this->view = new View($config);
// 改为（TP8 View 初始化方式变化）
\think\facade\View::config($config);
$this->view = \think\facade\View::instance();
```

### 23b：改造 common.php（addons 加载器）

来源：`vendor/karsonzhang/fastadmin-addons/src/common.php`
目标：`application/common/addons/common.php`

**核心替换**：

```php
// 原
use think\Hook;
use think\Loader;
use think\App;

Route::any('addons/:addon/...', "\\think\\addons\\Route@execute");
Loader::addNamespace('addons', ADDON_PATH);
Hook::listen('addon_init');
Hook::add('app_init', function() {
    $config = App::$debug ? [] : Cache::get('addons', []);
    ...
    Hook::exec($v, 'app_init');
    Hook::import($hooks, true);
});
Hook::add('app_init', function() {
    ...
    Hook::listen($hook, $params);
});

// 改为
use think\facade\Route;
use think\facade\Cache;
use think\facade\Event;

Route::any('addons/<addon>/[<controller>/[<action>]]', "\\think\\addons\\Route@execute");
// Loader::addNamespace → composer.json autoload.psr-4 "addons\\\\" 处理，无需代码

// Hook::listen('addon_init') → Event::trigger('addon_init')
Event::trigger('addon_init');

// Hook::add('app_init', function(){...}) → 改写为在 middleware 中直接调用
// （addons 的 app_init hook 内容提取到 AppInit middleware 的末尾执行）

// App::$debug → app()->isDebug()

// Hook::exec($v, 'app_init') → Event::trigger('app_init', $v)

// Hook::import($hooks, true) → 自定义 EventImporter::import($hooks)

// Hook::listen($hook, $params) → Event::trigger($hook, $params)
```

### 23c：创建 addons/Route.php（路由执行器）

`vendor/karsonzhang/fastadmin-addons/src/addons/Route.php` 原样复制，改 `use` 声明：
```php
use think\facade\Request;
```

### 23d：更新 composer.json autoload

```json
"autoload": {
    "psr-4": {
        "app\\": "application/",
        "addons\\": "addons/",
        "think\\": "application/common/addons/",
        ...
    },
    "files": [
        "application/common/addons/common.php"
    ]
}
```
`"files"` 确保 addons loader 在请求启动时自动执行（与 TP5 的 common.php autoload 等价）。

**完成后**：更新 INDEX.md P0-23 → [x]

---

## P0-24 创建 application/provider.php

**目标文件**：`application/provider.php`（新建）

TP8 服务提供者绑定文件，目前项目暂无自定义 Provider，创建空文件即可：

```php
<?php
return [];
```

**完成后**：更新 INDEX.md P0-24 → [x]

---

## P0-25 冒烟测试 3 个入口

**执行**：
```bash
# 命令行测试
php -S 127.0.0.1:8099 index.php &
sleep 1
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8099/
# 期望：200 或 301/302（有安装锁）

# 直接 PHP CLI 验证框架加载
php -r "
define('ROOT_PATH', getcwd() . '/');
define('APP_PATH', getcwd() . '/application/');
define('RUNTIME_PATH', getcwd() . '/runtime/');
define('ADDON_PATH', getcwd() . '/addons/');
define('DS', DIRECTORY_SEPARATOR);
define('EXT', '.php');
define('ENTRANCE', 'index');
require 'vendor/autoload.php';
echo 'TP8 version: ' . \think\App::VERSION . PHP_EOL;
"
```

**通过标准**：
- 无 Fatal Error
- TP8 版本号输出为 `8.x.x`
- `curl` 返回非 5xx 状态码

**完成后**：更新 INDEX.md P0-25 → [x]，并将 INDEX.md P0 行进度改为 `25/25 ✅`
