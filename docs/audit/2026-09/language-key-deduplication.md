# 语言键去重

九份语言文件中余下的 14 组重复键均存在不同文字：九份 `menu/configuser`、德语 `menu/manga_data_points`，以及简体中文 `website`、`type_id`、`total`、`data`。按 PHP 当前实际生效的最后一条文字保留唯一声明，并保留该键首次出现的顺序。

使用 `tools/audit/language_keys.php` 分词读取全部 27,947 条语言声明，去重前后九份文件的有效关联数组序列化 SHA-256 完全相同；重复键从 14 降至 0。未执行语言源代码，也未重新翻译文字。该结论仅证明去掉隐式覆盖且维持当前展示合同，不能替代每种语言的措辞/语义校对。
