# 全量修复的刷新基线

固定源码 `4a7f90c7`（工作区清理完成后），工具版本、源码树、分类计数和日志 SHA-256 见 [JSON 记录](full-repair-baseline-20260911.json)。扫描不读取 Git 历史或生产配置。

| 检查 | 结果 |
| --- | --- |
| PHPStan level 1，目标 PHP 8.4 | 215 条文件诊断，0 全局分析错误 |
| PHPStan level 5，目标 PHP 8.4 | 978 条文件诊断，0 全局分析错误 |
| PHPCompatibility，PHP 8.3–8.4 | 0 错误，8 警告 |
| MySQL 8.0.46，PHP 8.3.33 | models / financial / upload / storage / remote_upload / install，71 个测试进程，0 失败 |
| MySQL 8.0.46，PHP 8.4.25 | 同上，71 个测试进程，0 失败 |

数据库新建于专用隔离网络和数据目录，没有公开端口；安装权限测试以非 root 身份运行。部分故障子进程按测试合同使用 SQLite 来核对真实提交状态；不能把上表解释为每个断言都在 MySQL 上执行。源码完整编译、默认套件、浏览器和文件来源检查的上一批结果另见 [清理记录](workspace-cleanup-20260911.md)，不混用源码树。

level 1 的主要类别是 105 条 Db 动态门面提示、38 条运行期常量、29 条分支变量、14 条语言覆盖、13 条 empty、8 条模型属性和其它 8 条。必须核对实际转发、初始化与消费路径，再决定修复或补充准确的分析声明。较高等级的 978 条是包含 level 1 的总数，不能相加成 1193 个问题。8 条兼容警告仍是构造函数 exit 的生命周期提示和配置混合换行。

随后独立完成两批封面修复：`b79959f2` 为原图组合备份字段迁移，`51f2efeb` 为生成与恢复事务。后者双版附件套件各 10 个进程全部通过，封面专项每版 SQLite 368 项、MySQL 严格/非严格模式各 369 项；包含实际提交前/后丢确认和独立读取物理结果。细节见 [封面事务报告](ai-cover-transactions.md)。这些专项结果不能代替后续最终全量复验。

审计仍在执行。剩余事务拥有权、持久幂等与核对、资源绑定、旧配置导入、隐式 DDL、模板输出和部署恢复验收继续按根因拆批。未知提交结果和旧数据库不一致记录不会自动删除或改写。
