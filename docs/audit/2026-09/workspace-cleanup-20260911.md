# 未提交工作区整理与验证（2026-09-11）

本轮按用户要求先收尾工作区。起点为 `f3f16ab84c2cce927fc5d364c18dc3e302d20182`，实际代码改动按责任边界分成以下 13 个提交，本记录与供应链说明另作一个文档提交。没有将全部修改合为一次提交。

| 提交 | 内容 |
| --- | --- |
| 588b871a | 审计临时缓存 |
| e2dfef32 | 配置纯数据加载 |
| c2aa9f11 | 退役更新与校验入口 |
| 572bcad2 | 本地资源站及关键词 |
| fb21a9ec | PHP 强制脚本策略 |
| 5f6f74c9 | 退役云插件与安装统计 |
| 8a2e2131 | 旧主题库与分享 |
| d4349483 | 编辑器资源本地化 |
| d88651ab | 播放器与演示资源 |
| 97caa0bd | 离线审计工具 |
| 7c28c45a | Apache/Nginx 静态 HTML |
| 1f2cf998 | 部署失败阻断 |
| f6e9634b | CI 与浏览器回归 |

Git 作者与提交者统一为 `ACCS2024 <160735780+ACCS2024@users.noreply.github.com>`。本轮不改写既有历史，不推送、不执行远端部署。

## 保全与清理

清理前的完整 tracked 二进制补丁、未跟踪源文件副本，以及验证日志保存在仓库外 `/home/dev/maccms-cleanup-20260911/`。删除 `tests/__pycache__/` 与 `tools/security/__pycache__/`，并加入 Python 字节码忽略规则。12 个只有换行变化的文件恢复 HEAD 格式，包括 `migration/lbjx9/index.php`；这些文件的实际代码未改动。其他源代码、依赖来源说明、许可证和回归文件均按批提交。

## 固定源码验证

PHP 全量编译和默认回归使用固定提交 `d88651ab55ab6a0fd5575120fce3ce1ee2838490`，tree `9aa866ad7fdbc4199a5213762da27ad54255ed2b`，独立只读导出并挂载干净锁定依赖。后续提交仅添加审计工具、部署/CI 与文档；Apache 测试文件的后续变化另在两个 PHP 版本编译并执行。

- PHP 8.3.33：包含干净锁定依赖的 5,431 个 PHP 文件编译通过，无诊断或跳过；默认回归 160 个独立进程通过，失败 0。
- PHP 8.4.25：包含干净锁定依赖的 5,431 个 PHP 文件编译通过，无诊断或跳过；默认回归 160 个独立进程通过，失败 0。

默认回归的模型和账务使用 SQLite，本次未重跑完整 MySQL 矩阵。两版本的配置、插件、安装、HTTP、资源站、CSP 定向验证通过；Apache 各 236 项，Nginx 39 项，Python 审计器 17 项均通过。浏览器覆盖真实交互、压缩、公式、表情、地图和播放，0 外部请求、0 未捕获异常；提交快照与已验证的前端文件字节一致。

源码策略扫描对应 tree `7a5bcadd89be0e64bd2df41eb3793ef1a9294529`（CI 提交），包含干净依赖，0 命中、6,413 个受检文件。文档不在源码规则扫描范围内；后续文档提交不改变该受检文件集合。审计日志、JSON 编译记录、基线和测试源版本在仓库外备份目录中保留，原始源码扫描结论另存于 [supply-chain-source-audit.json](supply-chain-source-audit.json)。

Composer 严格校验、ShellCheck、Bash 语法、CI YAML 解析通过。GitHub 托管 CI 与生产验收未执行。本轮没有重跑完整 PHPStan/PHPCompatibility；已登记的静态分析问题和后续业务/安全审计继续保留，工作区收尾不代表商业上线审计结束。

最终工作区验收使用 `git status --porcelain=v1 --untracked-files=all`，要求无输出；同时核对暂存区无差异，以及本轮全部提交的作者、提交者。
