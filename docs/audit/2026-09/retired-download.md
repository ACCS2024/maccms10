# 移除未使用的旧下载工具

移除 `application/common/util/Download.php`。检查当前仓库的 PHP、模板、配置、数据文件以及 JavaScript 中的完整类名、文件路径、大小写变体、实例化和字符串引用，没有发现当前业务调用；文件内只有类声明和同名方法。框架下载响应、对象存储下载以及视频下载页面使用各自实现，不依赖此类。

旧实现包含已失效的同名构造函数、未知扩展名/非图片文件的未定义数组读取、没有缓冲区时关闭缓冲，以及失败信息写入错误属性。没有发现连接该内部类的当前请求入口，因此不把这些库级问题描述为已证实的远程漏洞。

不在部署目录保留旧类副本。仓库外直接实例化 `app\common\util\Download` 或手动包含此文件的插件需要迁移：在自身权限检查及路径约束之后使用框架的 `think\response\File` 下载响应。当前仓库引用检查不能证明外部插件、生产数据库中的任意动态 PHP 或其他未提供文件没有依赖。

本退役组独立于仍由后台 `Cj` 使用的 `Collection` 修复；不移除或替换框架及 SDK 的下载功能。
