# 采集后的缓存清理结果

采集清理现在分别检查缓存驱动和页面临时目录的结果，捕获 `Throwable`，仅在两项均完成后输出成功并记录本实例已清理。缓存驱动失败不阻止页面临时目录清理；任一失败会输出失败信息，后续明确的清理调用可重试。成功后的重复调用不再删除新生成的缓存文件。

PHP 8.3、8.4 各执行 18 项检查：调用真实 Collect 私有方法，缓存边界注入 false、null、Exception、Error，实际文件系统验证目录不存在、误配置为文件、根链接及目标保留、失败恢复和成功后的防重复。另执行既有采集分页 21 项回归。本批不改变已完成的内容入库结果，也不将后续缓存清理错误冒充采集事务回滚；采集输出仍保留错误提示。
