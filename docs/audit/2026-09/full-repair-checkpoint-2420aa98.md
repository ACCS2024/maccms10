# 全量修复检查点：2420aa98

日期：2026-09-11。源码树 `51944c5b22e96190aa16f53e9d7fe171c50bbac0` 对应提交 `2420aa98`；以只读代码快照、隔离测试数据库及已验证的生产依赖执行。用户要求仍是全量修复，本检查点不代表工作完成。结构化统计和原始报告摘要见[JSON](full-repair-checkpoint-2420aa98.json)。

| 验证 | PHP 8.3.33 | PHP 8.4.25 |
| --- | --- | --- |
| 全量原生编译，含干净依赖及归档 | 5,460 文件，0 诊断 | 5,460 文件，0 诊断 |
| 默认独立回归 | 174 进程，0 失败 | 174 进程，0 失败 |
| 提现原 PDO 故障（MySQL） | 313 项 | 313 项 |
| 提现精确落库（MySQL 严格/非严格各跑一次） | 各 99 项 | 各 99 项 |
| 真实身份与私有缓存（MySQL） | index 247 / api 191 项 | index 247 / api 191 项 |
| 原生 OpenCC 1.1.9 CLI | 17 项 | 17 项 |

默认回归主要使用 SQLite。表中 MySQL 专项来自各自提交的冻结版本，并不把本次默认回归说成全 MySQL 回归。安装文件权限另以非 root 身份验证，构造响应批次双版各 22 项。新源码和干净生产依赖的来源策略检查 0 命中，生成 6,421 文件哈希；锁定主依赖及内嵌 Upyun 的在线 Composer audit 均未返回已知漏洞公告或弃用包。这些检查不能证明不存在未知漏洞或主机已安全。

PHPStan 1 级剩余 49 条、5 级剩余 798 条，均 0 全局分析错误，无 baseline/ignoreErrors。低级别主要是分支定义、Collection/已验证数组的重复空检查、未使用的变量清理/参数及归档迁移片段；更严格结果仍需核对实际 ORM 返回值、声明与业务行为。`a11caef7` 的声明校准只描述已存在的门面/初始化合同，不能把减少的诊断算成修复的漏洞。

PHPCompatibility 为 0 错误、1 告警：`application/extra/mctheme.php` 中一个字符串字面量包含两处 CRLF。已按 token 确认它们属于配置文本，不是可执行代码换行；不为消除格式告警而改变配置字符串，双版原生编译无诊断。其余七条构造退出告警对应的控制器已改为框架响应。

此后 `da6d98f7` 修正九份文件的 HTTP/PHPDoc 表述，逐文件确认可执行 token 相同，相关 5 级扫描无注释解析错误；`70760722` 清除当前实现忽略的 Request/插件额外参数，双版专项通过。它们没有冒用本次静态总数作为自身完整扫描结果。

本轮新增分批修复包括：封面完整组合备份与原子绑定，独立存储事务的未知结果处理，提现事务/精确写入，构造及前台守卫响应，语言键覆盖，后台监控、本机进程和 OpenCC，缓存命中及 404。每项均有独立提交与专项文档；没有推送或部署。

下一步优先检查提现会员/API/管理入口、历史保留及跨请求核对，随后继续共享缓存/身份、附件删除与采集资源绑定、请求中 DDL、普通保存的实际落库边界和其余模板上下文。已受自动审查限制的执行型复现未重试。原始证据存放于 `/home/dev/maccms-full-repair-20260911/checkpoint/`，不能用临时目录代替持续 CI 与生产验收。
