# 本轮分析 · ROUND-XXX

> 复制本模板到 `progress/round-log/ROUND-XXX.md`,**在动手改代码前**填完 A–F 并提交。
> 铁律 #1:没有这份分析,不许改一行。AI 执行时 A 段命令必须**真跑并贴输出**,不得凭记忆。

---

- **轮次**:ROUND-XXX
- **日期**:YYYY-MM-DD
- **所属阶段**:P_ · <阶段名>
- **本轮切片**:<一句话:动哪个模块/哪类 API/哪组文件>
- **风险级别**:🔴/🟠/🟢(见 `00-principles.md` §四)
- **执行人**:AI / 人

---

## A. 现状复扫(必须真跑命令,贴输出)

> 目的:用事实而非记忆确定本轮范围。粘贴实际命令与输出。

```bash
# 示例:本切片调用点计数 / 受影响文件
grep -rEn -e '<目标 API>' app --include=*.php | wc -l
# 示例:语法当前态
find app/<slice> -name '*.php' -print0 | xargs -0 -n1 php -l >/dev/null && echo lint-ok
```

输出:
```
<在此粘贴真实输出>
```

## B. 本轮改动清单(确切文件 + 计数)

| 文件 | 本轮改什么 | 调用点/行 |
|---|---|---|
| | | |

合计:__ 文件 / __ 处(确认 ≤ 阈值:~30 文件 / ~800 行,否则拆轮)

## C. TP5 → TP8 映射(本切片)

| 旧(TP5.0) | 新(TP8) | 注意 |
|---|---|---|
| | | |

(必要时贴关键新旧代码对照)

## D. 安全不变量影响

> 对照 `verification/security-invariants.md` 的 8 条,勾出本轮**触碰**的,并说明如何保持。

- [ ] INV-1 … - [ ] INV-2 … - [ ] INV-3 … - [ ] INV-4 …
- [ ] INV-5 … - [ ] INV-6 … - [ ] INV-7 … - [ ] INV-8 …

说明:<本轮如何确保被触碰的不变量不破>

## E. 验证计划

- 宿主 PHP8.4:`php -l` 范围 = ___ ;启动方式 = `php -S ...`
- 冒烟矩阵行:#__、#__(`verification/smoke-matrix.md`)
- 是否需 Docker 全栈:是/否(🔴 必须是)
- 黄金 diff:是/否
- 安全不变量复验项:INV-__

## F. 回滚点

- 起点 commit:`<hash>`
- 回滚方式:`git revert <本轮提交>`(本轮为单一聚焦提交)

---
---

## ⬇⬇ 以下在"实现+验证"后回填 ⬇⬇

## G. 验证结果(贴真实输出)

```
<php -l 结果 / 启动结果 / 冒烟结果 / 黄金 diff / 安全不变量结果>
```

## H. 残留 & 决策

- 残留项(同步登记到 PROGRESS 债务区):
- 偏离设计的决策与原因:

## I. 收尾

- [ ] PROGRESS.md 已勾选 / 残留已登记
- [ ] 聚焦提交:`<hash>`
- 轮次 DoD(`00-principles.md` §三):全部满足 ✅ / 未满足说明:
