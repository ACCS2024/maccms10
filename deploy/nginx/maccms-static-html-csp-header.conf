# Include beside the existing add_header directives in the serving server {}.
# Also include in HTML-serving child locations that define their own add_header;
# keep their other security headers. See docs/security/static-html-csp.md.
# Empty values add no header, preserving the application's approved script origins.
add_header Content-Security-Policy $maccms_static_html_csp always;
